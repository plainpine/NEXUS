# Nexus マッチングシステム 操作マニュアル

このシステムは、講師（先生）と生徒の最適な組み合わせを、コンピュータが自動で計算して決定するツールです。
「得意科目が一致しているか」「お互いの希望（性別や年代）が合っているか」を瞬時に判断し、バランスの良い組み合わせを作ります。

---

## 1. 準備：講師（先生）と生徒を登録する
最初に、通われている先生と生徒の情報を登録します。これは、新しい人が増えた時だけ行えばOKです。

### 講師（先生）の登録
1. 画面左側のメニューから「講師一覧」を選びます。
2. 「＋ 講師を追加」ボタンを押します。
3. 名前、性別、年代を入力し、**「得意科目」**を選びます（1:得意 〜 3:不得意）。
4. **「希望する生徒の条件」**を入力します。「中1〜中3のどの学年を優先的に担当したいか」を1（高い）〜3（低い）で設定します。
5. 「保存する」を押すと登録完了です。

### 生徒の登録
1. 画面左側のメニューから「生徒一覧」を選びます。
2. 「＋ 生徒を追加」ボタンを押します。
3. 名前、学年（中1〜中3）、性別を入力します。
4. **「希望する講師の条件」**を入力します。「どの年代の先生に教わりたいか」を1（高い）〜3（低い）で設定します。
5. 「保存する」を押すと登録完了です。

---

## 2. 毎日の操作：出席と科目の設定
授業が始まる前に、その日来ている人を確認します。

1. **出席チェック**: 
   - 「講師一覧」と「生徒一覧」で、今日出席している人の左側にある**「出席」のチェックボックス**をオンにします。
   - 画面下の「保存」ボタンを忘れずに押してください。
2. **学習科目の選択（生徒のみ）**:
   - 「生徒一覧」画面で、各生徒が**今日勉強する科目（最大2つ）**を選びます。
   - 選び終わったら、下の「出席と科目を保存」ボタンを押します。

---

## 3. 実行：マッチングを行う
準備ができたら、自動計算を開始します。

1. 画面左側のメニューから「マッチング実行」を選びます。
2. コンピュータが最適な組み合わせを計算します（数秒かかります）。
3. 計算が終わると、自動的に「マッチング結果」画面に移動します。

---

## 4. 確認：結果を見る
決定した組み合わせが表示されます。

- **講師順 / 生徒順**: 画面右上のボタンで、表示の並び順を切り替えられます。「先生ごとに誰を担当するか見たい」ときは講師順、「生徒が誰に教わるか見たい」ときは生徒順が便利です。
- **再実行**: 組み合わせをやり直したい場合は「再実行」ボタンを押してください。

---

## 困ったときは
- **「Not Found」と表示される**: 画面を一度読み込み直すか、左のメニューから別の画面に移動してみてください。
- **データが消えた**: 登録した情報は自動で保存されます。もし一覧に何も出ない場合は、画面上の「講師一覧」や「生徒一覧」を再度クリックしてください。
- **特定の人がマッチングされない**: その人の「出席」にチェックが入っているか確認してください。

---
**Nexus マッチングシステム**
